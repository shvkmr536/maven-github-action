capture:
  build:
    clean-command: mvn -B clean
    build-command: mvn -B -DskipTests package
  files:
    exclude-regex: |
      - "*.github"
      - "Comps/"
      - "Comps2/" 
  caching: 
    enabled: false 
